# 현재 작업 상태

> 이 문서는 프로젝트 진행 중 계속 갱신하는 상태 문서다.

## 1. 지금 하는 일

- 현재 작업: **게임 MVP 구현 + 플레이 검증 완료 (2026-07-07)**. 실제 수업 투입 전 사람 플레이 테스트 대기
- 현재 기준 파일/폴더: `app\` (게임 코드), `outputs\` (수업 문서)

## 2. 최근 완료한 것 (2026-07-07)

- ✅ 게임 전체 구현: 타이틀 → 로비+방 5개 탐험 → NPC 대화 → 퀴즈 20문항 → 배지/점수 → 엔딩 → 리스타트/이어하기
- ✅ 자동 플레이 검증 (브라우저 자동화로 시작~엔딩 전 구간):
  - 이동/충돌/걷기 애니메이션, 방 이동(문 6개), NPC 상호작용 힌트
  - 미션 5개 전부 완주 (오답 → 재도전 감점 채점까지 확인: 70+80×4 = 390/400)
  - 리스타트(저장 삭제), 이어하기(localStorage 복원), 터치 모드(가상패드+A버튼, 1024×768 태블릿 에뮬레이션)
  - 콘솔 에러 0, `npm run build` 통과 (TypeScript strict)
- ✅ 수업 문서 3종 (`outputs\`): 조작법(학생용), 수업활용가이드(45분 차시안+토론질문+문항수정법), 확장 미션 아이디어

## 3. 다음 할 일

1. 사람이 직접 플레이해보고 대사·문항 톤 확인 (자동 테스트는 로직만 검증함)
2. 실제 태블릿 기기에서 터치 확인 (에뮬레이션만 완료)
3. 수업 일정 확정 시: 배포 방식 결정 (교사 PC `npm run dev -- --host` 또는 빌드해서 내부 서버)
4. (선택) 확장 미션 추가 — `outputs\3-확장-미션-아이디어.md` 참고

## 4. 막힌 점 / 확인 필요

- 수업 일정 미정

## 5. 지금 기준 결정사항

- 미션 순서는 자유 (어느 방부터 가도 됨) — 조기 완료 학생 편차 흡수
- 채점: 문항당 첫 시도 20점 / 재도전 10점 / 3회째부터 5점 (총 400점) — 틀려도 배지는 동일하게 획득
- 퀴즈·대사는 `app\src\data\`에 분리 (교사가 문장만 수정 가능)
- 그래픽: Kenney Tiny Town + Tiny Dungeon (CC0), 폰트 Galmuri — 이모지는 배지/아이콘만

## 5-1. 계획과 달라진 점 (Deviations)

- **React StrictMode 이중 게임 루프 버그**: 개발 모드에서 effect 2회 실행 시 비동기 에셋 로딩 뒤 루프가 2개 돌아 이동 속도가 2배가 됨 → cleanup에서 `cancelled` 플래그로 로딩 완료 콜백 자체를 무시하게 수정 (`GameCanvas.tsx`). → **교훈: useEffect 안에서 async 작업 후 루프/구독을 시작하면 cleanup 레이스를 반드시 처리**
- **dev 전용 디버그 훅 추가**: 자동 플레이 검증용 `window.__game` (엔진·스토어 참조). `import.meta.env.DEV`에서만 노출되어 배포 빌드에는 포함 안 됨
- 퀴즈 문항 수: 지시서 "미션당 3~4문항" → 전 미션 4문항(총 20문항)으로 통일 (분량 균형)

## 6. 열어봐야 할 문서

- `AGENTS.md` / `PROJECT.md` / `구현지시서.md` / `SOURCES.md`
- `outputs\2-수업활용가이드.md` (수업 준비 시)

## 7. 결과물 정리 상태

- `assets\` 정리 여부: Kenney 팩 2종 (원본 zip + 압축해제본)
- `outputs\` 정리 여부: 수업 문서 3종 완료

## 8. 다음에 다시 시작할 때 메모

- 다음 시작점: `app\`에서 `npm run dev` → http://localhost:5173 에서 직접 플레이
- 주의할 점: 문항 수정은 `app\src\data\quizzes.ts`만 (correct: true는 문항당 1개)
- 우선순위: 사람 플레이 테스트 → 실기기 터치 확인 → 배포 방식 결정

## READ FIRST

- Workspace common entry: `D:\AI_Workspace\AGENTS.md`
- Workflow by task type: `D:\AI_Workspace\workflows\web.md`
- Project context: `PROJECT.md`, `SOURCES.md`, `구현지시서.md`

Index/screen-specific plan files:
- `구현지시서.md` — 게임 구현 단일 기준
